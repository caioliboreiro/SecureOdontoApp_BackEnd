services:
  pg:
    image: bitnami/postgresql:latest
    ports: 
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=docker
      - POSTGRESQL_PASSWORD=docker
      - POSTGRESQL_DATABASE=odonto-app
    volumes:
      - postgres_data:/bitnami/postgresql/data

volumes:
  postgres_data: